The lab's pull request workflow,
author and reviewer expectations,
and response protocol
are consolidated in @sec-collab-dev.

For code-specific prerequisites before requesting review,
use the quality checks in @sec-r-qa-checklist,
the documentation guidance in @sec-documenting-code,
and the ADR guidance in @sec-adrs
for design rationale that should outlive a merged PR thread.
