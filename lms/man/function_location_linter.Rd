% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/function_location_linter.R
\name{function_location_linter}
\alias{function_location_linter}
\title{Function-location linter}
\usage{
function_location_linter(allowed_dirs = c("R", "tests"))
}
\arguments{
\item{allowed_dirs}{Character vector of directory names in which top-level
function definitions are permitted. A file is exempt when any component of
its path matches one of these names. Defaults to \code{c("R", "tests")}: \verb{R/}
is where package functions belong, and \verb{tests/} is exempt because
\verb{helper-*.R} and setup functions there are standard testthat convention.}
}
\value{
A \code{\link[lintr:Linter]{lintr::Linter()}} object.
}
\description{
Flags \strong{top-level named function definitions} in files outside the
allowed directories. The lab convention is that named functions belong in a
package's \verb{R/} directory, one per file (see the lab manual's
\code{coding-practices/complete-package-development-workflow.qmd}), so a function
defined inline in a \verb{data-raw/} script, a vignette, or \verb{inst/} is usually
misplaced.
}
\details{
Matches the three named-definition forms — \verb{name <- function(...)},
\verb{name = function(...)}, and the \verb{name <- \\(...)} lambda shorthand. It does
\strong{not} flag anonymous functions passed as arguments
(\code{purrr::map(x, function(z) z)}), lambdas used inline, or nested/local
function definitions inside another function's body — only top-level
definitions bound to a name.

lintr has no built-in linter for function \emph{location}; this complements
\code{\link[=function_length_linter]{function_length_linter()}} (which flags over-long definitions wherever they
live). Genuine "unless absolutely necessary" exceptions use a \verb{# nolint}
marker or a repo-local \code{.lintr.R} exclusion.
}
