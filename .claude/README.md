# `.claude/`

Project-level Claude Code config for this repository.

Shared skills, hooks, rules, and workflows are provided via the `ai-config` plugin
([`Morrison-Lab/ai-config`](https://github.com/Morrison-Lab/ai-config))
rather than vendored as a git submodule.
